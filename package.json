{
  "name": "gitlab-ssrf-redirect-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "logs": "wrangler tail",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "vitest": "^5.0.0",
    "wrangler": "^4.130.0"
  }
}
